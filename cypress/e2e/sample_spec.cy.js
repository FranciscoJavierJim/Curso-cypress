describe('My First Test', () => {   //nos ayuda a darle un nombre a un grupo de test que estan dentro de un bloque
  it('Does not do much!', () => {   //nombramos el test
    expect(true).to.equal(true)     //nos da una asercion 
  })
})